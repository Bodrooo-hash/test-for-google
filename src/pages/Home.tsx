import DashboardLayout from "@/components/layout/DashboardLayout";

const Home = () => {
  return (
    <DashboardLayout breadcrumbs={[{ label: "Мой рабочий стол" }]}>
    </DashboardLayout>
  );
};

export default Home;
